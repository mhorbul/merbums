<% if is_admin? %>
<div>
  <p><%= link_to "Add a New Forum", url(:new_forum) %></p>
</div>
<% end %>
<table cellspacing="0">
  <thead>
    <tr>
      <th>Forums</th>
      <th class="last_update">Last Post</th>
    </tr>
  </thead>
  <tbody>
<% @forums.each do |forum| %>
  <tr>
    <td>
      <%= link_to forum.name, url(:forum,forum), :class => "forum_title" %>
      <p><%= forum.topics.size %> Topics - <%= forum.posts.size %> Posts</p>
      <%= forum.description_html %>
      <p><small></small></p>
    </td>
    <td class="last_update">
      <p>
        <%= forum.posts.size == 0 ? "Never" : "#{forum.latest_post.updated_at} + #{forum.latest_post.topic.title}" %>
      </p>
    </td>
  </tr>
<% end %>
  </tbody>
</table>